services:
  db:
    image: postgres:15-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-pisoftsolutions_production}
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d pisoftsolutions_production"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - my_network

  web:
    build: .
    command: sh -c "rm -f tmp/pids/server.pid && bundle exec rails tailwindcss:build && bundle exec rails s -p 3000 -b '0.0.0.0' -e ${RAILS_ENV:-development}"
    volumes:
      - .:/app
      - bundle_cache:/usr/local/bundle
      - node_modules_cache:/app/node_modules
    ports:
      - "3000:3000"
    environment:
      RAILS_ENV: ${RAILS_ENV:-development}
      RAILS_SERVE_STATIC_FILES: "true"
      RAILS_LOG_TO_STDOUT: "true"
      POSTGRES_HOST: db
      POSTGRES_PORT: 5432
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password}
      POSTGRES_DB: ${POSTGRES_DB:-pisoftsolutions_${RAILS_ENV:-development}}
      SECRET_KEY_BASE: ${SECRET_KEY_BASE}
      RAZORPAY_KEY_ID: ${RAZORPAY_KEY_ID}
      RAZORPAY_KEY_SECRET: ${RAZORPAY_KEY_SECRET}
      # Cashfree Configuration
      CASHFREE_APP_ID: ${CASHFREE_APP_ID}
      CASHFREE_SECRET_KEY: ${CASHFREE_SECRET_KEY}
      CASHFREE_ENVIRONMENT: ${CASHFREE_ENVIRONMENT:-sandbox}
      CASHFREE_WEBHOOK_SECRET: ${CASHFREE_WEBHOOK_SECRET}
      DEFAULT_PAYMENT_GATEWAY: ${DEFAULT_PAYMENT_GATEWAY:-cashfree}
      FORCE_SSL: ${FORCE_SSL:-false}
      RAILS_MAX_THREADS: ${RAILS_MAX_THREADS:-2}
      WEB_CONCURRENCY: ${WEB_CONCURRENCY:-1}
      # Email Configuration
      SMTP_ADDRESS: ${SMTP_ADDRESS}
      SMTP_PORT: ${SMTP_PORT}
      SMTP_USERNAME: ${SMTP_USERNAME}
      SMTP_PASSWORD: ${SMTP_PASSWORD}
      DOMAIN: ${DOMAIN}
      ADDITIONAL_DOMAINS: ${ADDITIONAL_DOMAINS}
      SUPPORT_EMAIL: ${SUPPORT_EMAIL}
    depends_on:
      db:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '0.5'
        reservations:
          memory: 512M
      restart_policy:
        condition: on-failure
        max_attempts: 5
    stop_grace_period: 30s
    stop_signal: SIGTERM
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - my_network

  nginx:
    build:
      context: .
      dockerfile: ./nginx.Dockerfile
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt  # Volume for Certbot certificates
      - ./scripts:/scripts  # Volume for scripts
      - ./public:/app/public
      - public-assets:/app/public/assets
      - ./public:/var/www/certbot  # Webroot for ACME challenges
      # - ./nginx/ssl:/etc/nginx/ssl
    depends_on:
      - web
    ports:
      - "80:80"
      - "443:443"     # HTTPS port
    environment:
      - RAILS_ENV=${RAILS_ENV:-development}
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "1"
    networks:
      - my_network

  # Certbot service for SSL certificates
  certbot:
    image: certbot/certbot
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt
      - ./public:/var/www/certbot
    command: certonly --webroot --webroot-path=/var/www/certbot --email ${SUPPORT_EMAIL} --agree-tos --no-eff-email -d ${DOMAIN} -d www.${DOMAIN} -d tech.easy2invest.co.in -d www.tech.easy2invest.co.in
    depends_on:
      - nginx
    networks:
      - my_network

networks:
  my_network:
    driver: bridge

volumes:
  postgres_data:
    driver: local
  bundle_cache:
    driver: local
  node_modules_cache:
    driver: local
  public-assets:
    driver: local
