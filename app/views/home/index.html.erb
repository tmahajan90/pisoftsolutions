<!-- Hero Section -->
<section class="bg-gradient-to-br from-indigo-50 to-purple-100 py-20">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
      <div>
        <h1 class="text-5xl lg:text-6xl font-bold text-gray-900 mb-6">
          Transform Your Business with 
          <span class="text-indigo-600">Innovative Software</span>
        </h1>
        <p class="text-xl text-gray-600 mb-8">
          🚀 PiSoftSolutions delivers cutting-edge software development, digital marketing, and business automation solutions to help your business thrive in the digital age.
        </p>
        <div class="flex flex-col sm:flex-row gap-4">
          <%= link_to "Start Your Project", "#", class: "bg-indigo-600 hover:bg-indigo-700 text-white px-8 py-4 rounded-lg font-semibold text-lg transition duration-300 text-center" %>
          <%= link_to "Schedule Consultation", "#", class: "border-2 border-indigo-600 text-indigo-600 hover:bg-indigo-600 hover:text-white px-8 py-4 rounded-lg font-semibold text-lg transition duration-300 text-center" %>
        </div>
        <div class="mt-8 flex items-center space-x-6">
          <div class="flex items-center">
            <i class="fas fa-phone text-indigo-600 mr-2"></i>
            <span class="text-gray-700">+1 (555) 123-4567</span>
          </div>
          <div class="flex items-center">
            <i class="fas fa-clock text-indigo-600 mr-2"></i>
            <span class="text-gray-700">24/7 Support</span>
          </div>
        </div>
      </div>
      <div class="relative">
        <div class="bg-white rounded-2xl shadow-2xl p-8 transform rotate-3 hover:rotate-0 transition-transform duration-300">
          <div class="flex items-center mb-4">
            <div class="w-3 h-3 bg-green-500 rounded-full mr-2"></div>
            <div class="w-3 h-3 bg-yellow-500 rounded-full mr-2"></div>
            <div class="w-3 h-3 bg-red-500 rounded-full"></div>
          </div>
          <div class="space-y-4">
            <div class="bg-gray-100 rounded-lg p-4">
              <div class="flex items-center mb-2">
                <div class="w-8 h-8 bg-indigo-500 rounded-full flex items-center justify-center mr-3">
                  <i class="fas fa-code text-white text-sm"></i>
                </div>
                <span class="font-semibold">Custom Software Development</span>
              </div>
              <p class="text-sm text-gray-600">Tailored solutions for your unique business needs</p>
            </div>
            <div class="bg-purple-50 rounded-lg p-4">
              <div class="flex items-center mb-2">
                <div class="w-8 h-8 bg-purple-500 rounded-full flex items-center justify-center mr-3">
                  <i class="fas fa-chart-line text-white text-sm"></i>
                </div>
                <span class="font-semibold">Digital Marketing</span>
              </div>
              <p class="text-sm text-gray-600">Data-driven strategies for business growth</p>
            </div>
            <div class="bg-green-50 rounded-lg p-4">
              <div class="flex items-center mb-2">
                <div class="w-8 h-8 bg-green-500 rounded-full flex items-center justify-center mr-3">
                  <i class="fas fa-cogs text-white text-sm"></i>
                </div>
                <span class="font-semibold">Business Automation</span>
              </div>
              <p class="text-sm text-gray-600">Streamline operations and boost efficiency</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Drive Innovation Section -->
<section class="py-20 bg-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <h2 class="text-4xl font-bold text-gray-900 mb-4">Drive Innovation</h2>
      <h3 class="text-3xl font-semibold text-indigo-600 mb-6">Empowering Businesses with Cutting-Edge Technology</h3>
      <p class="text-xl text-gray-600 max-w-3xl mx-auto">
        At PiSoftSolutions, we believe technology should be an enabler, not a barrier. Our team of expert developers, designers, and strategists work together to create innovative solutions that transform businesses and drive sustainable growth in today's competitive digital landscape.
      </p>
    </div>
  </div>
</section>

<!-- Products Section -->
<section class="py-20 bg-gray-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <h2 class="text-4xl font-bold text-gray-900 mb-4">Our Products</h2>
      <p class="text-xl text-gray-600">Discover our premium software solutions designed to boost your business growth</p>
    </div>
    
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 mb-12">
      <% @featured_products.each do |product| %>
        <div class="bg-white rounded-xl shadow-lg overflow-hidden hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1">
          <!-- Product Image -->
          <div class="relative">
            <div class="h-48 bg-gradient-to-br from-<%= product.color %>-500 to-<%= product.color %>-600 flex items-center justify-center">
              <i class="<%= product.image_url %> text-6xl text-white"></i>
            </div>
            
            <!-- Badge -->
            <div class="absolute top-3 left-3">
              <span class="bg-<%= get_badge_color(product.badge) %>-100 text-<%= get_badge_color(product.badge) %>-800 text-xs font-semibold px-2 py-1 rounded-full"><%= product.badge %></span>
            </div>
            
            <!-- Quick View Button -->
            <div class="absolute inset-0 bg-black bg-opacity-0 hover:bg-opacity-20 transition-all duration-300 flex items-center justify-center opacity-0 hover:opacity-100">
              <button onclick="window.location.href='<%= product_detail_path(product) %>'" class="bg-white text-gray-900 px-4 py-2 rounded-lg font-semibold transform translate-y-4 hover:translate-y-0 transition-all duration-300">
                <i class="fas fa-eye mr-2"></i>View Details
              </button>
            </div>
          </div>
          
          <!-- Product Info -->
          <div class="p-6">
            <!-- Category -->
            <div class="text-xs text-gray-500 uppercase tracking-wide mb-2"><%= product.category %></div>
            
            <!-- Product Name -->
            <h3 class="text-xl font-bold text-gray-900 mb-3 line-clamp-2"><%= product.name %></h3>
            
            <!-- Rating -->
            <div class="flex items-center mb-3">
              <div class="flex text-yellow-400 text-sm">
                <% product.rating.to_i.times do %>
                  <i class="fas fa-star"></i>
                <% end %>
                <% (5 - product.rating.to_i).times do %>
                  <i class="far fa-star"></i>
                <% end %>
              </div>
              <span class="text-sm text-gray-600 ml-2">(<%= product.rating %>)</span>
            </div>
            
            <!-- Price -->
            <div class="mb-4">
              <div class="flex items-baseline">
                <% 
                  # Get the appropriate price to display
                  display_price = if product.get_validity_options.any?
                    # Use the default validity option price, or the first one if no default
                    default_option = product.default_validity_option || product.get_validity_options.first
                    default_option&.price
                  else
                    # Use the product's base price if no validity options exist
                    product.price
                  end
                  
                  # Ensure we have a reasonable price (not 1.0 unless it's actually intended)
                  display_price = product.price if display_price == 1.0 && product.price != 1.0
                %>
                <span class="text-2xl font-bold text-<%= product.color %>-600">₹<%= display_price %></span>
                <span class="text-sm text-gray-500 ml-2">Starting from</span>
              </div>
              <% if product.original_price && product.original_price > product.price %>
                <div class="flex items-center text-xs text-green-600 font-semibold mt-1">
                  <span>Save ₹<%= (product.original_price - product.price).to_f %></span>
                </div>
              <% end %>
            </div>
            
            <!-- Validity -->
            <div class="mb-4">
              <div class="flex items-center text-xs text-gray-600">
                <i class="fas fa-clock text-blue-500 mr-2"></i>
                <% 
                  # Get the appropriate validity option to display
                  display_validity_option = if product.get_validity_options.any?
                    # Use the default validity option, or the first one if no default
                    product.default_validity_option || product.get_validity_options.first
                  else
                    nil
                  end
                  
                  # If we have a validity option, show its label, otherwise show a default message
                  validity_text = if display_validity_option
                    display_validity_option.label
                  else
                    "Lifetime"
                  end
                %>
                <span class="font-semibold">Validity: <%= validity_text %></span>
              </div>
            </div>
             
            <!-- Features -->
            <div class="space-y-1 mb-4">
              <div class="flex items-center text-xs text-gray-600">
                <i class="fas fa-check text-green-500 mr-2"></i>
                <span>Premium features</span>
              </div>
              <div class="flex items-center text-xs text-gray-600">
                <i class="fas fa-check text-green-500 mr-2"></i>
                <span>24/7 support</span>
              </div>
              <div class="flex items-center text-xs text-gray-600">
                <i class="fas fa-check text-green-500 mr-2"></i>
                <span>Money-back guarantee</span>
              </div>
            </div>
            
            <!-- Action Buttons -->
            <div class="flex gap-2">
              <button class="add-to-cart-btn flex-1 bg-<%= product.color %>-600 hover:bg-<%= product.color %>-700 text-white py-2 px-4 rounded-lg font-semibold text-sm transition duration-300 flex items-center justify-center cursor-pointer" data-product-id="<%= product.id %>">
                <i class="fas fa-shopping-cart mr-2"></i>
                Add to Cart
              </button>
              <button onclick="window.location.href='<%= product_detail_path(product) %>'" class="px-3 py-2 border border-<%= product.color %>-600 text-<%= product.color %>-600 hover:bg-<%= product.color %>-50 rounded-lg transition duration-300 cursor-pointer">
                <i class="fas fa-eye"></i>
              </button>
            </div>
          </div>
        </div>
      <% end %>
    </div>
    
    <!-- Show More Button -->
    <div class="text-center">
      <%= link_to products_path, class: "inline-flex items-center bg-indigo-600 hover:bg-indigo-700 text-white px-8 py-3 rounded-lg font-semibold text-lg transition duration-300" do %>
        <i class="fas fa-arrow-right mr-2"></i>
        View All Products
      <% end %>
    </div>
  </div>
</section>

<!-- Features Section -->
<section class="py-20 bg-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <h2 class="text-4xl font-bold text-gray-900 mb-4">Why Choose PiSoftSolutions?</h2>
      <p class="text-xl text-gray-600">The advantages that set us apart</p>
    </div>
    
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      <div class="text-center">
        <div class="w-20 h-20 bg-indigo-100 rounded-full flex items-center justify-center mx-auto mb-6">
          <i class="fas fa-users text-3xl text-indigo-600"></i>
        </div>
        <h3 class="text-xl font-bold text-gray-900 mb-4">Expert Team</h3>
        <p class="text-gray-600">Our team of certified professionals brings years of experience and expertise to every project.</p>
      </div>
      
      <div class="text-center">
        <div class="w-20 h-20 bg-purple-100 rounded-full flex items-center justify-center mx-auto mb-6">
          <i class="fas fa-rocket text-3xl text-purple-600"></i>
        </div>
        <h3 class="text-xl font-bold text-gray-900 mb-4">Fast Delivery</h3>
        <p class="text-gray-600">We follow agile methodologies to deliver high-quality solutions on time and within budget.</p>
      </div>
      
      <div class="text-center">
        <div class="w-20 h-20 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-6">
          <i class="fas fa-shield-alt text-3xl text-green-600"></i>
        </div>
        <h3 class="text-xl font-bold text-gray-900 mb-4">Quality Assurance</h3>
        <p class="text-gray-600">Rigorous testing and quality control processes ensure reliable and secure solutions.</p>
      </div>
      
      <div class="text-center">
        <div class="w-20 h-20 bg-blue-100 rounded-full flex items-center justify-center mx-auto mb-6">
          <i class="fas fa-headset text-3xl text-blue-600"></i>
        </div>
        <h3 class="text-xl font-bold text-gray-900 mb-4">24/7 Support</h3>
        <p class="text-gray-600">Round-the-clock technical support and maintenance to keep your systems running smoothly.</p>
      </div>
      
      <div class="text-center">
        <div class="w-20 h-20 bg-orange-100 rounded-full flex items-center justify-center mx-auto mb-6">
          <i class="fas fa-lightbulb text-3xl text-orange-600"></i>
        </div>
        <h3 class="text-xl font-bold text-gray-900 mb-4">Innovation Focus</h3>
        <p class="text-gray-600">We stay ahead of technology trends to provide cutting-edge solutions for your business.</p>
      </div>
      
      <div class="text-center">
        <div class="w-20 h-20 bg-red-100 rounded-full flex items-center justify-center mx-auto mb-6">
          <i class="fas fa-handshake text-3xl text-red-600"></i>
        </div>
        <h3 class="text-xl font-bold text-gray-900 mb-4">Client Partnership</h3>
        <p class="text-gray-600">We build long-term partnerships with our clients, understanding their evolving needs.</p>
      </div>
    </div>
  </div>
</section>

<!-- Stats Section -->
<section class="py-20 bg-indigo-600 text-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <h2 class="text-4xl font-bold mb-4">Trusted by Businesses Worldwide</h2>
      <p class="text-xl opacity-90">Our track record speaks for itself</p>
    </div>
    
    <div class="grid grid-cols-2 md:grid-cols-4 gap-8">
      <div class="text-center">
        <div class="text-4xl font-bold mb-2">500+</div>
        <p class="text-indigo-100">Projects Completed</p>
      </div>
      <div class="text-center">
        <div class="text-4xl font-bold mb-2">200+</div>
        <p class="text-indigo-100">Happy Clients</p>
      </div>
      <div class="text-center">
        <div class="text-4xl font-bold mb-2">50+</div>
        <p class="text-indigo-100">Team Members</p>
      </div>
      <div class="text-center">
        <div class="text-4xl font-bold mb-2">5+</div>
        <p class="text-indigo-100">Years Experience</p>
      </div>
    </div>
  </div>
</section>

<!-- CTA Section -->
<section class="py-20 bg-gray-50">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <h2 class="text-4xl font-bold text-gray-900 mb-6">Ready to Transform Your Business?</h2>
    <p class="text-xl text-gray-600 mb-8">
      Let's discuss how our innovative solutions can help you achieve your business goals and stay ahead of the competition.
    </p>
    <div class="flex flex-col sm:flex-row gap-4 justify-center">
      <%= link_to "Start Your Project", "#", class: "bg-indigo-600 hover:bg-indigo-700 text-white px-8 py-4 rounded-lg font-semibold text-lg transition duration-300" %>
      <%= link_to "Schedule Consultation", "#", class: "border-2 border-indigo-600 text-indigo-600 hover:bg-indigo-600 hover:text-white px-8 py-4 rounded-lg font-semibold text-lg transition duration-300" %>
    </div>
  </div>
</section>

<!-- CSS for line clamping -->
<style>
  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
</style>
